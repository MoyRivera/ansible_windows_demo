---
- name: Add a user
  hosts: all
  tasks:
    - name: Ensure user Ansible is present
      win_user:
        name: ansible
        password: @ns1bl3P4ssw0rd
        state: present
        groups:
          - Users
